import { render } from '@testing-library/vue'
import { describe, expect, it } from 'vitest';
import Nav from '../Nav.vue';

describe.skip('# Menu Navigation', () => {
  it(`should render the navigation menu`, () => {
  });
});